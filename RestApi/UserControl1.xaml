<UserControl x:Class="RestApi.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
               xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:RestApi"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#FFFFFF">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*">
            </RowDefinition>
            <RowDefinition Height="auto">
            </RowDefinition>
            <RowDefinition Height="auto">
            </RowDefinition>
        </Grid.RowDefinitions>
        <ListBox x:Name="MessagesList" Grid.Row="0" Grid.RowSpan="3" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,50">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel.ContextMenu>
                            <ContextMenu Background="White" Style="{StaticResource MaterialDesignMenu}">

                                <MenuItem x:Name="answer" Header="Ответить" Click="answer_Click">
                                    <MenuItem.Icon>
                                        <materialDesign:PackIcon Kind="Reply"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <MenuItem x:Name="edit" Header="Редактировать" Click="edit_Click" >
                                    <MenuItem.Style>
                                        <Style TargetType="MenuItem" BasedOn="{StaticResource MaterialDesignMenuItem}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMyMessage}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"> </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </MenuItem.Style>
                                    <MenuItem.Icon>
                                        <materialDesign:PackIcon Kind="Pencil"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <MenuItem x:Name="forward" Header="Переслать" Click="forward_Click">
                                    <MenuItem.Icon>
                                        <materialDesign:PackIcon Kind="Forward"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <MenuItem x:Name="delete" Header="Удалить" Click="delete_Click" DataContext="{Binding}">
                                    <MenuItem.Style>
                                        <Style TargetType="MenuItem" BasedOn="{StaticResource MaterialDesignMenuItem}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsMyMessage}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers> 
                                        </Style>
                                    </MenuItem.Style>
                                    <MenuItem.Icon>
                                        <materialDesign:PackIcon Kind="Delete"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                            </ContextMenu>
                        </StackPanel.ContextMenu>

                        <TextBlock Text="{Binding msg}" FontSize="14" TextWrapping="Wrap" Foreground="Black"  ></TextBlock>
                        <TextBlock Text="{Binding senddate}" FontSize="10" TextWrapping="Wrap" Foreground="Black" ></TextBlock>
                        <TextBlock Text="{Binding login}" FontSize="10" TextWrapping="Wrap" Foreground="Black" ></TextBlock>
                        <TextBlock Text="{Binding EditedAt}" FontSize="10" TextWrapping="Wrap" Foreground="Black" ></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Border x:Name="ActionPanel" Grid.Row="1" Background="#F0F4F8" BorderBrush="#0088CC" BorderThickness="3,0,0,0" Margin="0,0,50,5" Visibility="Collapsed" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon x:Name="ActionIcon" Kind="Reply" VerticalAlignment="Center" Foreground="#0088CC"/>
                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="ActionTitle" Text="Ответ" FontWeight="Bold" Foreground="#0088CC" FontSize="12"/>
                    <TextBlock x:Name="ActionText" Text="Текст..." Foreground="Gray"  FontSize="12" TextTrimming="CharacterEllipsis" MaxHeight="16"/>

                </StackPanel>
                <Button x:Name="cancel" Grid.Column="2" Style="{StaticResource MaterialDesignToolButton}" Width="25" Height="25" Click="cancel_Click" ToolTip="Отменить" >
                    <materialDesign:PackIcon Kind="Close" Width="15" Height="15" Foreground="Gray"/>
                </Button>
            </Grid>
        </Border>
        <TextBox  SpellCheck.IsEnabled="True" Language="ru-ru" x:Name="InputMsg" Grid.Row="2" Background="#80C8ED" Foreground="White" Width="auto" Height="50" Margin="0,0,50,0"  Style="{StaticResource MaterialDesignTextBoxBase}"></TextBox>
        <Button x:Name="sendMessage" Grid.Row="2"  Width="auto" Height="50" Focusable="True" ClickMode="Press"  Click="sendMessage_Click"  Background="White" Foreground="black" Style="{StaticResource MaterialDesignNumericUpDownButtonsStyle}" FontFamily="Segoe UI Variable Text Semibold" FontSize="14" Margin="750,0,0,0"  Content="Send"/>
        <TextBlock Text="Сообщение..." IsHitTestVisible="False" Foreground="Gray" Margin="10,15,591,0" Grid.Row="2"  >
            <TextBlock.Style >
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=InputMsg}" Value="">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
